x = linspace(0, 2*pi, 100);
#{
  Use linspace function to make a vector of equally spaced intervals
  (sometimes also called "linearly spaced values").
  The first value in the vector is 0, the final value is 2π and the vector contains 100 values.
#}

y = sin(x);
plot(x, y); # Plot!

#{
  This is an example of a block quote
#}

%{
  Hello there, it's me.
%} 
 